{{setPageTitle "Details Page"}}

<section id="deatils-page">
    <div class="wrapper">
        <div class="product-img">
            <img src="{{image}}">
        </div>
        <div class="product-info">
            <div class="product-text">
                <div class="product-text">
                    <h1>Name: {{name}}</h1>
                    <h2>Type: {{type}}</h2>
                    <h4>Year: {{year}}</h4>
                    <h4>City: {{city}}</h4>
                    <p>Description: {{description}}</p>
                    
                    {{#if tenants}}
                        <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
                        <p>People rented this housing: {{tenants}}</p>
                    {{else}}
                        <p>People rented this housing: There are no tenants yet.</p>
                    {{/if}}
                </div>
            </div>

            {{#if user}}
                <div class="product-btn">
                    {{#if isOwner}}
                        <a href="/housings/{{_id}}/edit" class="edit">Edit</a>
                        <a href="/housings/{{_id}}/delete" class="remove">Delete</a>
                    {{else}}
                        {{#if availablePieces}}
                            {{#unless isRented}}
                                <a href="/housings/{{_id}}/rent" class="rentHome">Rent a home, available {{availablePieces}} housing</a>
                            {{else}}
                                <p class="alRentHome">You have already rent this home</p>
                            {{/unless}}
                        {{else}}
                            {{#if isRented}}
                                <p class="alRentHome">You have already rent this home</p>
                            <!-- logged in user with no available pieces-->
                            {{else}}
                                <p class="no-housing">No Housing Available!</p>
                            {{/if}}
                        {{/if}}
                    {{/if}}
                </div>
            {{/if}}
        </div>
    </div>
</section>